pipeline {
    agent any
    stages {
        stage('Checkout') {
            checkout scm
        }
        stage('Convert') {
            steps {
                // convert sass files
                // package django container
                // run tests
                // deploy container
                // 
            }
        }
    }
}
